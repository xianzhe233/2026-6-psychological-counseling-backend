<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tyut.psychological.common.notification.mapper.NotificationLogMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO notification_log (
            user_id, notify_type, title, content, create_time
        ) VALUES (
            #{receiverUserId}, #{notifyType}, #{title}, #{content}, NOW()
        )
    </insert>

    <select id="selectByStudentId" resultType="com.tyut.psychological.common.notification.vo.StudentNotificationVO">
        SELECT 
            id,
            title,
            content,
            notify_type,
            create_time,
            0 AS read_status
        FROM notification_log
        WHERE user_id = #{studentId}
        ORDER BY create_time DESC
    </select>

    <select id="countByStudentId" resultType="long">
        SELECT COUNT(*)
        FROM notification_log
        WHERE user_id = #{studentId}
    </select>

</mapper>