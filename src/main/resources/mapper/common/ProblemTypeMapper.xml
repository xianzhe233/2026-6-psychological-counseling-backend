<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tyut.psychological.common.mapper.ProblemTypeMapper">

    <!-- 查询所有启用的问题类型 -->
    <select id="selectAllEnabled" resultType="com.tyut.psychological.common.entity.ProblemType">
        SELECT id, type_name, sort_order, status, create_time
        FROM problem_type
        WHERE status = 1
        ORDER BY sort_order ASC
    </select>

    <!-- 根据ID查询问题类型 -->
    <select id="selectById" resultType="com.tyut.psychological.common.entity.ProblemType">
        SELECT id, type_name, sort_order, status, create_time
        FROM problem_type
        WHERE id = #{id}
    </select>

</mapper>
